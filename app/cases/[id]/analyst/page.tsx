import Link from"next/link";import{notFound}from"next/navigation";import{db}from"@/lib/db";import{AnalystChat}from"@/components/AnalystChat";
export default async function Analyst({params}:{params:Promise<{id:string}>}){const{id}=await params;const c=await db.case.findUnique({where:{id}});if(!c)notFound();return <main className="min-h-screen p-6 md:p-10"><div className="mx-auto max-w-5xl"><Link href={`/cases/${id}`} className="text-sm text-slate-400">← {c.title}</Link><p className="mt-6 text-xs tracking-[.3em] text-cyan-300">EVIDENCE-BACKED ANALYST</p><h1 className="mt-2 text-3xl font-semibold">Ask TRACY</h1><p className="mt-2 text-sm text-slate-400">Answers are generated from structured case records and point back to evidence when available. This first version is deterministic; an LLM layer can be added after the evidence contract is stable.</p><div className="mt-8 rounded-2xl border border-slate-800 bg-slate-950/40 p-6"><AnalystChat caseId={id}/></div></div></main>}
